// Copy to Configs/Local.xcconfig (gitignored) and fill in your values, then
// re-run `xcodegen generate`. Signing with a stable personal identity keeps
// macOS permission grants (Screen Recording etc.) across rebuilds.
DEVELOPMENT_TEAM = YOUR_TEAM_ID
